# Database Configuration
# Choose DB_TYPE: 'sqlite' (default) or 'mysql'
DB_TYPE=sqlite

# --- SQLite Configuration ---
# (Optional) Path to the SQLite database file, relative to project root.
# Defaults to 'db/agora.db' if commented out or missing.
# DB_SQLITE_PATH=db/agora.db

# --- MySQL Configuration ---
# Required only if DB_TYPE=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306 # Optional, defaults to 3306 if omitted
# DB_USER=your_mysql_user
# DB_PASSWORD=your_mysql_password
# DB_NAME=your_mysql_database
